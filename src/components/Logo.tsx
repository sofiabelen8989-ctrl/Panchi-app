import { motion } from "motion/react";

export const DachshundLogo = ({ className }: { className?: string }) => (
  <img
    src="/panchi-logo.png"
    alt="Panchi Logo"
    className={className}
  />
);
